{
  "name": "momentum-goal-system",
  "version": "4.0.0",
  "private": true,
  "description": "An adaptive, local-first goal operating system built for OpenAI Build Week.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check planner.js && node --check app.js && node --check api/coach.js"
  },
  "engines": {
    "node": ">=18"
  }
}
